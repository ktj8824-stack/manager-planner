# 🎬 매니저플래너 (ManagerPlanner) 고도화 개발 로드맵

본 문서는 엔터테인먼트 아티스트 스케줄 및 현장 매니저 동선 관리 플랫폼을 위한 종합 개선 계획서 및 진행 기록입니다.

---

## 🚀 단계별 로드맵

### 🚩 [Phase 1] 도메인 100% 정제 및 골프 잔재 박멸 (✅ 완료)
- [x] **용어 및 메타데이터 정비**:
  - `manifest.json`, `privacy.html` 내 PWA 메타정보를 **'매니저플래너 (ManagerPlanner)'**로 완전히 통일.
  - `profile.js`의 기본 사용자명을 `'현장 매니저'` (`manager@star-ent.com`)로 수정.
- [x] **체크리스트 전면 교체**:
  - 엔터테인먼트 현장 실무 맞춤형 체크리스트 (의상 착장/신발 픽업, 최신 큐시트/대본, 헤메 수정 파우치, 아티스트 음료/상비약, 차량 주유/하이패스, 법카/영수증 등).
- [x] **PRO 멤버십 화면 (`upgrade.js`) 리뉴얼**:
  - 기획사/매니저 관점의 기능(무제한 스케줄, 야외 세트장 초정밀 날씨, 단톡방 브리핑 텍스트 생성, 광고 제거/본사 긴급 알림)으로 혜택 재구성.
- [x] **주요 거점 데이터베이스 개편 (`data.js`)**:
  - 기존 골프장(CC) 데이터를 방송국(상암 SBS/MBC/CJ, 여의도 KBS, 일산 드림센터, 목동 SBS), 청담 샵거리, 주요 공연장(올림픽공원, 고척돔, 인스파이어), 세트장 등 18개 핵심 거점으로 전면 교체.
- [x] **스태프 풀 및 타임라인 문구 정비**:
  - 스태프: 헤어디자이너, 메이크업 실장, 스타일리스트 팀장, 전담 경호팀, 총괄 PD, 로드 매니저
  - 타임라인: 클럽하우스/환복 ➡️ 현장 도착/대기실 세팅 및 인이어/마이크 준비, 스케줄 시작 & 무대 진행.

---

### 🚩 [Phase 2] 맛집 및 주변 시설 탭 개편 (🚫 필요 없음 - 기능 삭제됨)
- [x] **기능 삭제 완료**:
  - `restaurant.js`, `restaurant.css` 파일 완전 삭제.
  - `index.html` 하단 탭 및 내비게이션 바에서 진입점 제거.
  - `app.js` 등 라우팅 로직에서 맛집 관련 코드 일괄 정리.

---

### 🚩 [Phase 3] 일정 등록 화면 (`register.js`) 고도화 (✅ 완료)
- [x] **엔터테인먼트 전용 폼 추가**:
  - 아티스트 선택 (루나스, 에이펙스, 강서준 등 드롭다운/칩)
  - 스케줄 분류 (음악방송, 화보/촬영, 공연/행사, 방송/라디오, 미팅)
  - 샵(헤메) 필요 여부 체크박스 및 샵 장소/시간 설정
  - 배차 차량 선택
- [x] **본사 스토어 (`hqStore`) 및 Supabase와 통합 저장**:
  - 매니저가 폰에서 추가해도 본사 관제 화면에 실시간으로 반영되도록 단일 데이터 파이프라인 구축.

---

### 🚩 [Phase 4] 매니저 실무 킬러 기능 탑재 (✅ 완료)
- [x] **📋 카카오톡 일정 브리핑 텍스트 원클릭 복사**:
  - 아티스트 및 스태프 단톡방에 매일 밤 보내는 "내일 스케줄 공지 포맷" 자동 생성 및 원클릭 복사 완료.
- [x] **🚗 내비게이션 딥링크 강화**:
  - 카카오내비 뿐만 아니라 티맵(TMAP) 원터치 길안내 지원 (네이버지도는 요청에 의해 제외).
- [x] **⚡ 실시간 상태 버튼 개선**:
  - [픽업 완료] ➔ [샵 도착] ➔ [현장 도착] ➔ [귀가 완료] 등 각 단계 클릭 시 현재 시각(타임스탬프)이 기록되며 본사에 전송되는 로직 개선.

---

### 🚩 [Phase 5] 4대 역할 권한 통제(RBAC), 비공개 스케줄(Secret Mode) 및 UI/UX 튜닝 (✅ 완료)
- [x] **4대 직책별(Role) 맞춤 권한 분리**:
  - `👑 대표(CEO)`: 전사 스케줄, 비공개 일정, 전사 통계/분석 열람.
  - `🏢 본사 총괄(HQ Admin)`: 전체 스케줄 등록/수정/배차 관제, 비공개 스케줄 등록/관리.
  - `🚗 현장 매니저(Manager)`: 배정된 아티스트 중심 스케줄, 타임스탬프 갱신, 카톡 브리핑, 내비게이션.
  - `🎧 외부 스태프/게스트(Staff)`: 불필요한 내부 메모/배차가 제외된 당일 타임라인·콜타임 중심의 클린 뷰어 모드.
- [x] **원클릭 역할 전환기 (Role / Persona Switcher)**:
  - 모바일 프로필 화면(`profile.js`), 모바일 홈 헤더, 본사 관제 포털(`admin.html`) 상단에서 1초 만에 역할을 자유롭게 전환하며 실시간 권한을 체감할 수 있는 시뮬레이터 구축.
- [x] **🔒 비공개 스케줄 (Secret / Confidential Mode)**:
  - 스케줄 등록(`register.js` 및 `admin.js`) 시 비공개 스케줄 지정 토글 지원.
  - 권한이 없는 일반 매니저/외부 스태프에게는 `🔒 [비공개 보안 일정]`으로 마스킹 및 상세 정보 접근 제한.
- [x] **모바일 & 본사 대시보드 UI/UX 디테일 튜닝**:
  - 헤더 역할 뱃지, 비공개 스케줄 전용 보라색/레드 뱃지 및 그라디언트 카드 스타일, 반응형 레이아웃 폴리싱.

---

### 🚩 [Phase 6] API 모듈 정제 및 레거시 잔재 완전 박멸 (✅ 완료)
- [x] **API 모듈 (`api.js`) 정제**:
  - 카카오/티맵 장소 검색 로직에서 골프장(CC) 검색 잔재 및 불필요한 분기문 완전 제거.
  - 엔터테인먼트 스케줄 장소(방송국, 세트장, 샵, 공연장 등) 전용 장소 검색 및 현장 긴급 편의시설(`searchNearbyConvenience`) 지원 체계로 전환.
  - API 호출 실패 시 주요 방송 거점 fallback 데이터 검색 안정화.
- [x] **워크스페이스 임시 파일 정리**:
  - 개발 과정에서 생성된 임시 스크립트(`temp.js`, `test_api.js`, `replace_terms.cjs`, `replace_terms.ps1`) 안전하게 제거 완료.

---

### 🚩 [Phase 7] 실무 기능 보강 - 중복 배차 충돌 경고 및 타임스탬프 상세 히스토리 로그 (✅ 완료)
- [x] **🚨 차량/매니저 중복 배차 충돌 감지 (Conflict Detection)**:
  - `HQDataStore.checkConflict()`: 동일 날짜/겹치는 시간대에서 차량 또는 담당 매니저가 중복 배정될 경우 실시간 감지.
  - 모바일 일정 등록/수정(`register.js`) 및 본사 관제 포털(`admin.js`)에서 일정 저장 시 충돌 경고 컨펌 대화상자 표시.
  - 본사 관제 일정 상세 팝업(`admin.js`) 상단에 중복 배차 충돌 시각적 경고 배너 렌더링.
- [x] **⏱️ 실시간 타임스탬프 상세 히스토리 로그 기록**:
  - 모바일 타임라인(`home.js`)에서 [이동중] / [완료] 터치 시 실제 기록 시간(`doneAt: '14:25'`)을 타임라인 카드에 `⏱️ 14:25 기록` 뱃지로 표시.
  - `HQDataStore.addStatusLog()`를 통해 각 단계별 시간, 일자, 매니저명, 상태 변경 내역을 히스토리 로그(`statusLogs`)로 영구 보존.
  - 본사 관제 상세 모달에서 실시간 타임스탬프 이력 목록 렌더링 지원.
- [x] **📲 서비스 워커(PWA) 오프라인 캐시 목록 최신화 (`sw.js`)**:
  - 삭제된 과거 파일 제거 및 `admin.html`, `admin-data.js`, `supabase-client.js` 등 신규 필수 파일 캐싱 완료.

---

### 🚩 [Phase 8] 보안 & Supabase RLS 정책 고도화 및 비공개 스케줄 DB 동기화 (✅ 완료)
- [x] **4대 역할(Role) DB 스키마 확장 (`supabase_schema.sql`)**:
  - `profiles.role` 제약조건을 `('ceo', 'hq_admin', 'manager', 'staff')` 4대 직책으로 전면 확장.
- [x] **🔒 비공개 스케줄(Secret Mode) DB 테이블 및 RLS 보안 정책 구축**:
  - `schedules` 테이블에 `is_secret`, `secret_level`, `status_logs`, `departure_info`, `outfit`, `supplies` 컬럼 완비.
  - `is_ceo_or_hq_admin()` 함수 및 RLS 정책을 통해 CEO/HQ는 전체 접근, 일반 매니저는 배정 스케줄, 외부 스태프는 `is_secret = false`인 공개 스케줄만 조회 가능하도록 DB 레벨 원천 차단.
- [x] **클라이언트 ↔ Supabase 양방향 동기화 모듈 보강 (`admin-data.js`, `supabase-client.js`)**:
  - 스케줄 생성/수정/조회 시 비공개 여부 및 타임스탬프 로그까지 완벽히 클라우드 동기화 연동.

---

### 🚩 [Phase 9] 레거시 잔재 파일 완전 박멸 및 클린업 (✅ 완료)
- [x] **미사용 레거시 챗봇 파일 제거**:
  - `js/chat.js`, `css/chat.css` 삭제 완료.
- [x] **앱 아이콘 SVG 리뉴얼 (`icon.svg`)**:
  - 기존 골프 깃발/골프공 그래픽을 엔터테인먼트 매니저플래너 슬레이트(클래퍼보드) & 스타 엠블럼 기반 네온 다크 디자인으로 전면 교체.
- [x] **도메인 주석 및 참조 변수 정비**:
  - `timeline.js`, `timeline.css`, `app.js`, `data.js` 내 주석 및 레거시 골프 참조 문구 정비.
- [x] **전체 JS 파일 문법 및 안정성 검증**:
  - `node --check`를 통한 전 모듈 문법 무결성 확인 완료.

---

### 🚩 [Phase 10] Supabase Realtime 크로스 디바이스 실시간 푸시 & 오프라인 동기화 (✅ 완료)
- [x] **본사 긴급 공지(Announcements) DB 테이블 및 Realtime 활성화 (`supabase_schema.sql`)**:
  - `announcements` 테이블 신설 및 `supabase_realtime` 퍼블리케이션 등록.
- [x] **본사 관제 포털 실시간 공지 발송 UI & 백엔드 연동 (`admin.html`, `admin.js`)**:
  - `modal-send-message` 모달 및 폼 완비, 수신 대상(전체/개별 매니저) 선택, 긴급 공지 토글, `createAnnouncement()` 및 `BroadcastChannel` 이중 전송 구현.
- [x] **모바일 ↔ 본사 실시간 스케줄 변경 & 긴급 알림 자동 감지 (`app.js`, `admin.js`, `supabase-client.js`)**:
  - 모바일에서 상태 변경 시 본사 관제 화면이 1초 만에 자동 리렌더링.
  - 본사에서 공지 발송 시 모바일 화면에 실시간 상단 토스트 팝업(`showHqToast`) 및 진동 안내 지원.
- [x] **네트워크 온라인/오프라인 자동 감지 및 복구 동기화 (`app.js`)**:
  - 지하 주차장/세트장 등 오프라인 시 로컬 보관 안내 및 온라인 복구 시 자동 클라우드 동기화 flush 구현.

---

### 🚩 [Phase 11] 엔터테인먼트 실무 킬러 기능 (예정)
- [ ] **영수증 및 지출 간이 정산**: 주유/식대/통행료 사진 촬영 및 엑셀 정산.
- [ ] **아티스트 케어 & 라이더 카드**: 알레르기, 선호 음료, 특이사항 1초 뷰어.
- [ ] **차량 운행일지 & 주차 위치 메모**: 방송국 지하 주차 구역 사진/메모.


